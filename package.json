{
  "name": "interactive-keyboard",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "dependencies": {
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^38.1.2",
    "prettier": "3.6.2"
  },
  "scripts": {
    "start": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "electron": "electron .",
    "electron-dev": "concurrently \"npm start --prefix frontend\" \"npm run electron\"",
    "backend": "cd python_light_server && python3 light_server.py",
    "dev": "concurrently \"npm run backend\" \"npm start --prefix frontend\""
  }
}
